# Backend

This folder contains the API and server-side architecture for URMIS.

## Intended structure
- config/: environment and service configuration
- database/: connection, migrations, and seeders
- middleware/: auth, tenant, permissions, uploads, and error handling
- auth/: authentication routes, services, and validation
- modules/: role-based modules with controllers, routes, services, repositories, validation, and policies
